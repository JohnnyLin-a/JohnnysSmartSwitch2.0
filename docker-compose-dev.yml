version: '3.8'
services:
  ss_backend:
    build:
      context: .
      dockerfile: ./Dockerfile.dev
    container_name: ss_backend
    restart: unless-stopped
    volumes:
      - "/app/node_modules"
      - "./:/app"
    ports:
      - "3000:3000"